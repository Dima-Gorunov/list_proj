// import { toast } from "react-toastify";

// export const toastSuccess = (text: string) => {
//     toast.success(text, { position: "top-right", autoClose: 3000, theme: "colored", type: "success" });
// };

// export const toastError = (text: string) => {
//     toast.error(text, { position: "top-right", autoClose: 3000, theme: "colored", type: "error" });
// };
