version: '3'
services:
  postgres_db:
    container_name: "postgres_db"
    image:  postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=dima
      - POSTGRES_PASSWORD
      - 