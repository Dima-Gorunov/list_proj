const { File } = require("../models/models");
const jwt = require("jsonwebtoken");

const { JWT_REFRESH_STRING, JWT_ACCESS_STRING } = require("../constant");

class FileService {
    async isMyFile(userId) {}
}

module.exports = new FileService();
